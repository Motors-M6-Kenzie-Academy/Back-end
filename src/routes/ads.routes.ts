import { Router } from "express";
// import adsCreateController from "../controllers/Ads/createAds.controller";

const routes = Router();

export const adsRoutes = () => {
  //   routes.post("/", adsCreateController);
  //   routes.post("/:id", adsCreateController);

  return routes;
};
